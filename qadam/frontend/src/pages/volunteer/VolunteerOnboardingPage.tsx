// frontend-routes.md "/volunteer/onboarding" — the real MultiStepForm
// (personal info -> skills & interests -> map pin -> review) is Phase 3
// scope. This stub exists only so VolunteerGuard has a redirect target.
export default function VolunteerOnboardingPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8 text-center">
      <p className="text-muted-foreground">
        Volunteer onboarding form goes here (Phase 3).
      </p>
    </main>
  );
}
