// frontend-routes.md "/ngo/onboarding" — the real NgoOnboardingForm is
// Phase 3 scope. This stub exists only so NgoGuard has a redirect target.
export default function NgoOnboardingPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8 text-center">
      <p className="text-muted-foreground">NGO onboarding form goes here (Phase 3).</p>
    </main>
  );
}
